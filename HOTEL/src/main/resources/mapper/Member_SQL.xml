<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatos.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="member">
 <!-- 회원가입 -->
<insert id="insertOneMember" parameterType="<!-- 자바빈 경로 -->" resultType="<!-- 자바빈 경로  -->">
<![CDATA[
        INSERT INTO MEMBER(MEM_ID, MEM_TYPE, MEM_NAME, MEM_USERID, MEM_PW, MEM_PHONE, MEM_EMAIL) 
		VALUES(SEQ_MEM_ID.NEXTVAL, #{mem_type}, #{mem_name}, #{mem_userid}, #{mem_pw}, #{mem_phone}, #{mem_email})
]]>
</insert>
 <!-- 로그인 -->
<select id="selectOneMember" parameterType="<!-- 자바빈 경로  -->" resultType="<!-- 자바빈 경로  -->">
<![CDATA[
	SELECT MEM_ID, MEM_TYPE, MEM_NAME, MEM_USERID, MEM_PW, MEM_PHONE, MEM_EMAIL, MEM_VISIBLE, MEM_CHG_DATE, MEM_REV_COUNT FROM MEMBER WHERE MEM_USERID = #{mem_userid}
]]>
</select>
<!-- 아이디 찾기 -->
<select id="findOneMember" parameterType="<!-- 자바빈 경로  -->" resultType="<!-- 자바빈 경로  -->">
<![CDATA[
SELECT MEM_ID, MEM_TYPE, MEM_NAME, MEM_USERID, MEM_PW, MEM_PHONE, MEM_EMAIL, MEM_VISIBLE, MEM_CHG_DATE, MEM_REV_COUNT FROM MEMBER WHERE MEM_NAME = #{mem_name} AND MEM_EMAIL = #{mem_email}
]]>
</select>


</mapper>